const Student={
    name:"Anique",
    age:20,
    eng: 30,
    Math:20,
    Java:31,
getavg(){
   let avg= (this.eng, +  this.Java, + this.Math )/3;

   console.log( `${this.name} Got the average = ${avg}`)

}

}